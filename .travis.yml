language: python

python:
  - '3.6'

services:
  - postgresql
  - redis-server

env:
  -DJANGO=2.0.5 DB=mysql
    
install:
  - pip install -r requirements.txt

before_script:
  - psql -c 'create database arkiot;' -U postgres